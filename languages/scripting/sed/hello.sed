#!/bin/sed -f
# Sed Hello World
# Sed (Stream EDitor) is a Unix utility for parsing and transforming text
# The 'i' command inserts text before the current line
# Since sed processes input, we use it to insert our message

i\
Hello, World!
